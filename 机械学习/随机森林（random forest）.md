# 放回抽样（sampling with replacement）
即每进行一次抽取后放回样本
# 随机森林
本质上是通过放回抽样的树集成算法，其效果远优于使用单一决策树
1. 首先通过放回抽样原数据集的内容进行m次获得一个新的数据集
2. 再通过这个新的数据集训练一个决策树模型
3. 不断进行前两个步骤直到训练完成
在对每个树进行决策分裂时，可以通过确定n个特征中的k个特征来指定分裂特征，通常为$k=m^{1/2}$.
与决策树相比，随机森林通过训练多次探索了许多对数据的微小变化，并且平均了这些微小变化